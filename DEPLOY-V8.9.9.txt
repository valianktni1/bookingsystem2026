BOOKINGSYSTEM2026 V8.9.9 - TRUENAS / DOCKGE DEPLOYMENT

1. Upload the complete contents of this ZIP to the main branch of:
   https://github.com/valianktni1/bookingsystem2026

2. Run these commands in the TrueNAS shell:

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml down
docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml build --no-cache app
docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml up -d

3. Wait for healthy status, then check:

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml ps
curl http://127.0.0.1:30049/api/health

Expected build:
2026.08.15-cancel-close-balance-v8.9.9

4. Hard refresh the browser once after deployment.

No YAML or database-reset commands are required.
No invoice counters, bookings, payments, imported records or documents are reset.
Deployment sends no emails and changes no calendars.
