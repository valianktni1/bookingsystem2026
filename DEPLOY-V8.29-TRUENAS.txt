BOOKINGSYSTEM2026 V8.29 - DURABLE CLIENT FORM DRAFTS

1. Extract the supplied complete V8.29 ZIP.
2. Replace the GitHub repository contents with the extracted contents.
3. Commit and push to the main branch using GitHub Desktop.
4. Run these commands in the TrueNAS shell:

cd /mnt/apps/dockge/data/bookingsystem2026

BOOKING_BACKUP="/mnt/apps/dockge/data/bookingsystem2026/bookingapp-before-v829-$(date +%Y%m%d-%H%M%S).dump"

docker exec bookingsystem2026-db pg_dump -U bookingapp -d bookingapp -Fc \
  > "$BOOKING_BACKUP"

ls -lh "$BOOKING_BACKUP"

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml build --no-cache app

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml up -d app

curl -sS http://127.0.0.1:30049/api/health

Expected build:

"build":"2026.08.25-durable-form-drafts-v8.29"

After deployment, open a TEST booking's client link on a phone. Enter a few
answers in the Wedding Booking Form or Final Wedding Timings, move to another
step, close the browser, then reopen the same link. The saved answers and the
same step should be restored. Do not submit a real couple's form as a test.
