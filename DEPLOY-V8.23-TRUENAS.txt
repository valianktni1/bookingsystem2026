BOOKINGSYSTEM2026 V8.23 - TRUENAS SHELL COMMANDS

1. In GitHub Desktop, unzip/open this V8.23 folder, commit it and push it to main.

2. Then run these commands in the TrueNAS shell:

cd /mnt/apps/dockge/data/bookingsystem2026

docker exec bookingsystem2026-db pg_dump -U bookingapp -d bookingapp -Fc > bookingapp-before-v823-2026-08-20.dump

ls -lh bookingapp-before-v823-2026-08-20.dump

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml build --no-cache app

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml up -d app

curl -sS http://127.0.0.1:30049/api/health

3. The health response should include:

"build":"2026.08.20-final-call-pack-v8.23"

No YAML or environment changes are needed for V8.23.
