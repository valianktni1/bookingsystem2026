BOOKINGSYSTEM2026 V8.9.7 — COMPLETE CUMULATIVE DEPLOYMENT

Upload the complete ZIP contents to the main branch of:
https://github.com/valianktni1/bookingsystem2026

No YAML additions are needed for this version. Keep all existing SMTP, IMAP,
Google Maps, database and storage settings exactly as they are.

Rebuild:

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml down
docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml build --no-cache app
docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml up -d

Check:

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml ps
curl http://127.0.0.1:30049/api/health

Expected build:
2026.08.09-editable-web-enquiry-form-v8.9.7

After signing in, open:
Setup > Web enquiry form

Use Preview live form before making a change, then Save & publish. The iframe
already embedded on your website will update automatically; do not replace it.

This deployment does not send client emails, enable imported Studio Ninja
automation or contact Google Calendar.
