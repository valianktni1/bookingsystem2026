BOOKINGSYSTEM2026 V8.28.1 - PUBLIC ENQUIRY EMBED HOTFIX

1. Extract the supplied V8.28.1 ZIP.
2. Publish all extracted contents to the main branch using GitHub Desktop.
3. Wait until GitHub shows that commit on main.
4. Run in the TrueNAS shell:

cd /mnt/apps/dockge/data/bookingsystem2026

docker exec bookingsystem2026-db pg_dump -U bookingapp -d bookingapp -Fc \
  > bookingapp-before-v8281-2026-08-24.dump

ls -lh bookingapp-before-v8281-2026-08-24.dump

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml build --no-cache app

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml up -d app

curl -sS http://127.0.0.1:30049/api/health

5. Confirm the response contains:

"build":"2026.08.24-enquiry-embed-hotfix-v8.28.1"

6. Open https://perfectweddingsbymark.uk/contact/ in a private/incognito browser
window. The full enquiry form should now appear in place of the refused message.

This deployment sends no email and changes no booking, invoice, payment,
Google Calendar or Accounts data.
