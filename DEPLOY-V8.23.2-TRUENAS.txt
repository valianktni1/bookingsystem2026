BOOKINGSYSTEM2026 V8.23.2 - URGENT FINAL TIMINGS MOBILE FIX

1. In GitHub Desktop, replace/open the repository with this V8.23.2 folder,
   review the small update, commit it and push it to main.

2. Then run these commands in the TrueNAS shell:

cd /mnt/apps/dockge/data/bookingsystem2026

test ! -e bookingapp-before-v8232-2026-08-21.dump || { echo "STOP: backup already exists"; exit 1; }

docker exec bookingsystem2026-db pg_dump -U bookingapp -d bookingapp -Fc > bookingapp-before-v8232-2026-08-21.dump

pg_restore -l bookingapp-before-v8232-2026-08-21.dump >/dev/null && echo "Database backup checked OK"

ls -lh bookingapp-before-v8232-2026-08-21.dump

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml build app

docker compose -f /mnt/apps/dockge/data/bookingsystem2026/compose.yaml up -d app

curl -sS http://127.0.0.1:30049/api/health

3. The health response must include:

"build":"2026.08.21-final-timings-mobile-fix-v8.23.2"

No YAML, environment or database migration change is needed for V8.23.2.

After the health check, open the affected couple's Final Timings link in a fresh
browser tab. The browser will fetch the new cache-busted client files. Complete a
quick test before asking the couple to try again.
